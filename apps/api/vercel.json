{
  "version": 2,
  "installCommand": "cd ../.. && npm install --include=dev",
  "buildCommand": "npm run build",
  "outputDirectory": "public",
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index"
    }
  ]
}
