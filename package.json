{
  "name": "studioflow",
  "version": "0.1.0",
  "private": true,
  "description": "Music collaboration platform monorepo",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @studioflow/api & npm run dev --workspace @studioflow/web",
    "dev:web": "npm run dev --workspace @studioflow/web",
    "dev:api": "npm run dev --workspace @studioflow/api",
    "build": "npm run build --workspace @studioflow/shared && npm run build --workspace @studioflow/api && npm run build --workspace @studioflow/web",
    "lint": "npm run lint --workspace @studioflow/api && npm run lint --workspace @studioflow/web",
    "test": "npm run test --workspace @studioflow/api"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
